# Assume a VPC exists with a public subnet and 3 private subnets.
# Each subnet has a route table.
# The public subnet as an internet gateway and the private subnets have a NAT each.
# Each NAT has an elatstic IP.
